package com.universal.reconciliation.domain.enums;

/**
 * Defines how a field participates in reconciliation.
 */
public enum FieldRole {
    KEY,
    COMPARE,
    DISPLAY,
    PRODUCT,
    SUB_PRODUCT,
    ENTITY,
    CLASSIFIER,
    ATTRIBUTE
}
