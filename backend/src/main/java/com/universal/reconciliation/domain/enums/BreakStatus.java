package com.universal.reconciliation.domain.enums;

/**
 * Enumerates the lifecycle states for a reconciliation break item.
 */
public enum BreakStatus {
    OPEN,
    PENDING_APPROVAL,
    REJECTED,
    CLOSED
}
