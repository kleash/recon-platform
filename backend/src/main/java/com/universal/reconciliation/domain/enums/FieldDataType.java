package com.universal.reconciliation.domain.enums;

/**
 * Enumerates the supported logical data types that the matching engine
 * understands when evaluating configured comparison rules.
 */
public enum FieldDataType {
    STRING,
    DECIMAL,
    INTEGER,
    DATE
}

